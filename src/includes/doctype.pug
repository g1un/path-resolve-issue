mixin doctype()
    doctype html
    html
        head
            title "Подари библиотеке книгу"
            meta(charset="UTF-8")
            meta(name="viewport", content="width=device-width")
            //favicon start
            link(rel="apple-touch-icon", sizes="144x144", href="/apple-touch-icon.png")
            link(rel="icon", type="image/png", sizes="32x32", href="/favicon-32x32.png")
            link(rel="icon", type="image/png", sizes="16x16", href="/favicon-16x16.png")
            link(rel="manifest", href="/site.webmanifest")
            link(rel="mask-icon", href="/safari-pinned-tab.svg", color="#5bbad5")
            meta(name="msapplication-TileColor", content="#da532c")
            meta(name="theme-color", content="#ffffff")
            //favicon end

            //social share links start
            meta(property="og:title", content="Подари библиотеке книгу")
            meta(property="og:description" content="Нашей сельской библиотеке очень нужна ваша помощь")
            meta(property="og:url", content="books.uralbiblio.ru")
            meta(property="og:image" content="img/about-1@2x.png")
            //social share links end

            link(rel='stylesheet', type='text/css', href='css/main.css')
        body
            block
            script(src='js/main.js')

            // Yandex.Metrika counter
            script(type="text/javascript").
                (function (d, w, c) {
                    (w[c] = w[c] || []).push(function () {
                        try {
                            w.yaCounter50454226 = new Ya.Metrika2({
                                id: 41750234,
                                clickmap: true,
                                trackLinks: true,
                                accurateTrackBounce: true
                            });
                        } catch (e) {
                        }
                    });

                    var n = d.getElementsByTagName("script")[0],
                        s = d.createElement("script"),
                        f = function () {
                            n.parentNode.insertBefore(s, n);
                        };
                    s.type = "text/javascript";
                    s.async = true;
                    s.src = "https://mc.yandex.ru/metrika/tag.js";

                    if (w.opera == "[object Opera]") {
                        d.addEventListener("DOMContentLoaded", f, false);
                    } else {
                        f();
                    }
                })(document, window, "yandex_metrika_callbacks2");
            noscript
                div
                    img(src="https://mc.yandex.ru/watch/41750234", style="position:absolute; left:-9999px;", alt="")
            // /Yandex.Metrika counter

            // Google Analytics
            script.
                (function (i, s, o, g, r, a, m) {
                    i['GoogleAnalyticsObject'] = r;
                    i[r] = i[r] || function () {
                            (i[r].q = i[r].q || []).push(arguments)
                        }, i[r].l = 1 * new Date();
                    a = s.createElement(o),
                        m = s.getElementsByTagName(o)[0];
                    a.async = 1;
                    a.src = g;
                    m.parentNode.insertBefore(a, m)
                })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

                ga('create', 'UA-29187770-1', 'auto');
                ga('send', 'pageview');
            // End Google Analytics
